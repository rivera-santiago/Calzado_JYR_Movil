
// Placeholder removed per user request. This route file remains to satisfy
// expo-router's requirement for a default export. It intentionally renders
// nothing; remove this file only if you also remove the corresponding route
// from your navigator.
import { View } from 'react-native'

export default function HomePlaceholder() {
	return <View />
}
